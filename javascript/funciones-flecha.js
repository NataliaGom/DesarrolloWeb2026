const saludar = (nombre) => {
  return "Hola, " + nombre;
};

const sumar = (a, b) => a + b;

const esPar = (numero) => {
  return numero % 2 === 0;
};

const celsiusAFahrenheit = (celsius) => (celsius * 9) / 5 + 32;